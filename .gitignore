# Tajemství — NIKDY necommitovat
.env

# Instanční konfigurace (verze, URL specifikací) — šablona je pins.example.json
config/pins.json

# Stažené OpenAPI specifikace (Atlassianova díla)
spec/

# Výstupy sondování API (dočasné, obsahují data z instancí)
probe/

# macOS
.DS_Store

# MCP config obsahuje absolutní cestu — šablona je .mcp.example.json
.mcp.json
